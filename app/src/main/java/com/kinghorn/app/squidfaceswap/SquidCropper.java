package com.kinghorn.app.squidfaceswap;

import android.content.Context;

public class SquidCropper extends SquidCanvas {
    public SquidCropper(Context con, SquidBitmapData f) {
        super(con, f);
    }
}
